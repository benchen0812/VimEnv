#### Install Step

    $ cd ~
    $ mv .vim vim-backup
    $ git clone git@github.com:benchen0812/VimEnv.git .vim
    $ ln -s .vim/vimrc.vim .vimrc
    $ ln -s .vim/.zshrc .zshrc


#### Enter vim

:PlugInstall
